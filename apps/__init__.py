"""
Feature apps live here. Each app is a vertical slice (URLs, views, forms,
services, repositories, schemas). There are no Django ORM models.py files
for business collections — MongoDB contracts belong in schemas.py.
"""
